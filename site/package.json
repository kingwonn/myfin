{
  "name": "myfin-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "deploy": "npx wrangler pages deploy dist --project-name myfin"
  },
  "devDependencies": {
    "marked": "^15.0.0",
    "yaml": "^2.6.0"
  }
}
