# 奖励函数 v1.0 总设计（SOTA 拼装 + 我们的纪律）

## 小白版：这是给交易算法定的"考试评分标准"

四条打分规则，训练出一只**稳健而不是莽撞**的算法：
1. **赚钱加分——但按风险打折**：在颠簸股票上赚的钱，得分打折（防止算法去赌过山车）
2. **亏钱重罚——亏得越深罚得越狠**：回撤惩罚是凸的（亏 10% 的罚分远超亏 5% 的两倍）
3. **每笔交易先交"过路费"**：按真实成本模型扣分（含"买多了自己推高价格"的冲击成本），治好频繁交易多动症
4. **危险时刻逞英雄=直接大负分**：黑天鹅状态机亮黄灯/红灯时还加仓，一票重罚

## 形式定义

```
R_t = w_r·f(r_vol) − w_d·g(DD_t) − w_c·Cost_t − λ_e·EventPenalty_t + HardRules
```

| 项 | 定义 | SOTA 来源（验证票数） |
|----|------|---------------------|
| f(r_vol) | 多周期前向收益(3/7/15日,权重0.3/0.5/0.2) ÷ 20期滚动波动率，**非对称映射**（负端更陡） | Trading-R1 (3-0) + M1不对称实证 |
| g(DD) | 凸回撤惩罚：g = (DD/DD_max)²·κ；触及风控阶梯(-10/-15/-20%)分档跳升，-20%回合终止+大负分 | RA-DRL 融合思路 + docs/risk-rules §5 |
| Cost | 固定费+点差下限+平方根冲击项 σ·√(x/V)（小资金也保守计入） | MACE/Almgren-Chriss (3-0) |
| EventPenalty | 状态机乘子：NEUTRAL=0；ELEVATED/SHOCK 中持有超限敞口按超出比例罚分 | event-risk-layer.md (M1 实证) |
| HardRules | ①黄/红灯加仓→大负分；②杠杆动作不存在于动作空间（多头单纯形+现金，GIFT 式）；③换手率超帽→罚分 | 我们的铁律 |

## 反"钻空子"(reward hacking)设计

- 各分量有界（clip），防单项刷分
- 动作空间物理排除杠杆与做空——不是罚，是**做不到**
- 波动率归一化使"躲进低波动刷微利"与"赌高波动搏大赢"同样无利可图

## 治理（凌驾于训练之上）

1. 权重 (w_r,w_d,w_c,λ_e) 单纯形网格搜索——**每一组都计入试验次数 N**，最终报 DSR（回测协议 v1.1）
2. 验证一律 CPCV；Walk-Forward 不得单独使用
3. **全领域零实盘证据的现实**（Q-SOTA 3 票确认）→ 本奖励函数只用于纸面前向实验评分；通过 Q1-Q3 阶段门前不碰真钱
4. 事件层参数缺口（M1b 补证中）期间取保守值

## 版本
- v1.0 (2026-07-11 马拉松 M-2/M-3)：设计定稿。M-3 交付 Python 骨架 + 合成数据自检。
- **v1.1 (2026-07-12 评审整改)**：①f(r_vol) 明确为**组合耦合**——r_vol = 持仓权重 · 逐资产前向分数，现金贡献 0（评审 R-1：v1.0 实现与持仓解耦，空仓满仓同分，已修）；②λ_event 只在汇总时乘一次（评审 R-2：v1.0 双重计费，SHOCK 实际 4x，已修）；③-20% 终止的语义：分量保持有界，回合级 DD_TERMINAL_PENALTY 由汇总层单独施加；④**裁判分离**：不变量测试 `test_reward_invariants.py` 由未读实现代码的独立代理从本规格派生（信念 B10），实现方不得自写验收测试。已知限制：相邻步前向窗口重叠会重复计分（G-3），打分用可接受，RL 训练版须去重。

## v1.2 修订队列（红队 2026-07-12 驱动，按治理顺序执行）

> 顺序铁律：①先在本节定稿规格修订 → ②独立代理更新裁判套件（红队场景固化为回归不变量）→ ③再改实现。跳过任何一步=运动员兼裁判复辟。

1. **H1 波动率同期化**：f(r_vol) 的归一化分母改为与前向收益同窗口的已实现波动率（现为滞后 20 期，同收益可套 4 倍分，奖励"平静期埋伏"）。
2. **H2 持有超帽累罚**：event 惩罚随停留在超帽状态的步数递增（现只罚加仓瞬间，score>1.333 时硬扛净正、反罚合规减仓者）。
3. **H3 成分风险项**：事件层加持仓加权波动率或 HHI 集中度罚分（现 SHOCK 帽内全押最热名零罚且多得 +0.72/步）；与 E3 个股帽合并实现。
4. **H5 终止惩罚单调化**：DD_TERMINAL_PENALTY 调整为 ≤ 任何非终止步最坏值（现 dd=0.19 比跌穿 0.20 更痛 3.83 分，教算法"跌穿止损封顶损失"）。
5. **H4/H6 引擎化**：以 RewardEngine 类持有 book 计价单位与回合级高水位，消灭"自报口径"面（v1.1 已加 prior_peak 与单位契约文档作过渡）。
6. **G-3 前向窗口去重**：RL 训练版对相邻步重叠的前向收益去重/折扣。

v1.1 已当场落地（不改奖励形状）：M1 σ/adv/book 非法值抛异常、M2 价格正性断言、M3 波动率窗口不足拒绝打分、L1/L2 边界校验、H6 过渡版 prior_peak、H4 单位契约文档化。红队完整报告：`redteam-2026-07-12.md`。

## v1.2 规格定稿（2026-07-12 晚，治理第①步；裁判套件更新=第②步；实现=第③步）

> 本节为可测试规格：每条带公式与边界。红队 H1/H2/H3/H5/H4/H6 逐条对应。

### S1（对 H1）波动率同期化
f(r_vol) 各周期 h∈{3,7,15} 的归一化分母改为**同窗前向已实现波动率**：
`vol_fwd_h = std(log 日收益, 窗口 [t, t+h])`，`z_h = fwd_ret_h / max(vol_fwd_h,1e-8) / sqrt(h)`。
不变量：同一前向路径（收益与前向波动相同）在任何滞后历史（平静/喧闹）下得分**相同**；滞后 20 期波动率不再进入 f(r_vol)（仍可用于成本项 σ 的缺省估计）。负端 ×ASYMMETRY、逐资产 clip、组合加权、现金=0 均不变。

### S2（对 H2）超帽停留累罚
引擎维护 dwell 计数 k：状态∈{ELEVATED,SHOCK} 且 gross>cap 的**连续**步数（离开状态或降到帽内即清零）。
`event_ratio = (over/cap) × min(1 + 0.5×(k−1), 4.0)`（k 从 1 计）。
不变量：k=1 时与 v1.1 相同；同 over 下第 3 步惩罚 = 第 1 步 ×2.0；封顶 ×4；合规减仓到帽内后 k 归零。

### S3（对 H3）成分集中度项
`HHI = Σ(w_i/gross)²`（gross>0；gross=0 时定义 HHI=0）。
非 NEUTRAL 态：`event_ratio += clip((HHI − 0.5) × 2, 0, 1) × (gross/cap)`。
不变量：SHOCK 帽内(0.5) 单票全押（HHI=1）产生正惩罚 ≈ λ×1.0×(0.5/0.5)=λ×min(1,·)——即"崩盘中全押最热名"不再免费；两票均分（HHI=0.5）不罚；NEUTRAL 态不罚集中度（集中度纪律平时由 risk-rules 管，事件层只管危险时刻）。

### S4（对 H5）终止惩罚单调化
`DD_TERMINAL_PENALTY = −25.0`（低于任何非终止步可达最坏值 ≈ −23）。
不变量：对任意固定的(状态,动作)，dd 从 0.19 → 0.21 的 total **单调变差或相等**；terminal 时 total==−25。

### S5（对 H4/H6）RewardEngine 引擎化
新增 `RewardEngine(book_currency_unit, adv_unit_check=True)`：持有**回合级高水位**（不可被截断曲线绕过）、event dwell 计数、book/adv 单位一致性断言（adv_value 与 book_value 必须同单位，比值超出 [1e-4, 1e6] 视为单位错报并抛异常）。
`engine.step(prices, t, equity_point, weights_prev, weights_new, event_state, sigma_daily, adv_value) -> RewardResult`——equity 只需传当前点，高水位由引擎累积。函数式 API 保留用于单步评估，但**回合评估以引擎为准**；drawdown_penalty 的 prior_peak 参数保留兼容。

### S6（对 G-3）训练模式去重
`mode="train"` 时 f(r_vol) 只用 h=3 单周期（步进 dt≥3 则无重叠）；`mode="eval"`（默认）保留三周期加权。不变量：train 模式相邻步（间隔≥3）的奖励对同一段未来不重复计分。

### 兼容性说明（裁判须知）
- v1.1 套件中受影响的断言：inv3（λ 精确差分需乘 dwell/HHI 修正，k=1、两票均分时数值不变）、inv4（不对称测试改用前向同窗波动构造）、inv5/inv8（终止值 −10→−25）、契约常量表（DD_TERMINAL_PENALTY）。其余不变量应全部保持。

## v1.3 修订队列（红队二轮 2026-07-12，奖励形状类——治理顺序执行）

1. **N1 HHI 伪分散**：单票占 gross <70.7% 即零集中罚（一大仓+碎仓拆分实测罚 0.32→0.00）→ 集中度度量改 top-1 占比或有效标的数（√HHI），或加单票硬上限项。
2. **N2 vol 地板满分爆炸**：平滑路径 +0.003% 与 +1.5% 同为满分 3.0（幅度失明），train 模式全暴露 → 地板改收益量纲相对下限，或 clip 触顶样本降权。
3. **N6 dwell 震荡重置**：gross∈(0.5,0.75) 缺口带在 SHOCK↔ELEVATED 震荡下累罚失效（SHOCK 日 dwell 恒 1）→ 清零条件改"降到帽内或 NEUTRAL"，或按最严帽判 over。
4. N8（=G-3 锐化）：eval 重叠最多 15× 计分——回合累计/模型选择场景禁用 eval 求和，只用 train 模式或去重后序列。
- 当场已修（不改形状）：N3 引擎 reset() + N5 终止闭锁 + N4 equity 正性校验 + N7 σ=0 拒收。红队二轮完整报告：redteam2-2026-07-12.md。
