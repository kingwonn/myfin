# CLAUDE.md — AI 运行时操作手册

本仓库是一个 AI-native 投研系统（"言出法随"）。你（Claude Code）是这个系统的运行时。用户用自然语言下达研究/跟踪/复盘指令，你负责把它变成对本仓库的读写与外部数据的获取。

## 语言约定

- 所有面向用户的输出、文档、报告：**中文**。
- 代码、YAML 字段名、commit message：英文。

## 数据与文件约定

- `watchlist/*.yaml`：结构化跟踪清单。修改时保持 schema 一致（见文件头注释）。新增标的必须填 `moat`（护城河）与 `risk`（证伪条件）字段，缺一不可。
- `theses/`：一个论点一个文件，命名 `YYYY-MM-DD-<slug>.md`，必须从 `TEMPLATE.md` 派生，必须包含"我错了的情形"章节。
- `docs/research/`：深度研究报告，命名 `YYYY-MM-DD-<主题slug>.md`，关键数字必须带来源链接。
- `journal/`：决策与复盘，命名 `YYYY-MM-DD.md`，追加不改写——历史记录不许美化。

## 研究纪律（对你的硬性要求)

1. **关键数字必须有来源**：市占率、产能、估值、订单金额——写进报告前先核实，标注来源与日期。
2. **区分事实与推断**：报告中用"[事实]"与"[推断]"标记，或在行文中明确区分。
3. **不回避坏消息**：如果研究发现某个论点被证伪，第一时间在对应 thesis 文件的状态栏标记 `falsified` 并写明原因。
4. **不给确定性承诺**：任何收益预期都要写明前提假设。

## 高频动作（技能）

- `/scan`：遍历 watchlist，搜索每个标的的最新关键动态，输出变化摘要，更新 `last_checked` 字段。
- `/thesis`：新建或评审论点，强制走 TEMPLATE 的证伪检查。
- `/brief`：汇总活跃论点状态 + 近期关键事件，生成简报存入 `journal/`。

## 工具与数据源优先级

1. 已集成的开源管道（`pipelines/`，随建设逐步丰富）
2. WebSearch / WebFetch（新闻、公告、财报）
3. 需要新数据源时：先查 `docs/02-tech-stack.md` 里的候选开源项目，优先复用

## 禁止事项

- 不执行任何真实交易指令（本系统只做研究与决策支持）。
- 不在报告中编造数据；查不到就写"未能核实"。
